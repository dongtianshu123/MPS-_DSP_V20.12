# Software Revision Skill — 软件系统修订技能说明文档

## Skill 名称

`software-revision`

## 定位

对嵌入式 C 代码项目（dsPIC、ARM、DSP、MCU 固件）进行系统化的分析、修订、优化和文档输出。基于 MPS 高压固态软起动器 DSP 项目（dsPIC30F5011，约 5000 行 C 代码）的完整实践提炼。

---

## 触发条件

当用户提出以下任一请求时，自动触发本 skill：

- "分析这个代码库" / "analyze this codebase"
- "修订这个项目" / "refactor this project"
- "优化系统流程" / "optimize the system"
- "添加全面注释" / "add comprehensive comments"
- "改进代码质量" / "improve code quality"
- "修复编码问题" / "fix encoding issues"
- "生成修订报告" / "create a revision report"
- "清理代码" / "clean up code"
- "使这个固件达到生产级别" / "make this production-ready"
- "现代化这个固件" / "modernize this firmware"
- 提到"MPS 工作流"/"软件修订方法论"/多阶段代码改进计划

---

## 核心原则

| 原则 | 说明 |
|---|---|
| **渐进式工作** | 每阶段基于前一阶段的成果，保存到独立版本化文件夹（如 `_refactored/`、`_V20.03_optimized/`） |
| **保持行为一致** | 所有修改必须保持功能等价，除非明确提升安全性（如启用看门狗） |
| **全程文档化** | 每阶段输出详细 Markdown 报告，说明变更内容、原因和前后对比 |
| **向后兼容** | 通讯协议、EEPROM 布局和外部接口必须保持兼容 |

---

## 六阶段方法论

### 第一阶段：项目理解与编码修复

**目标**：理解完整代码库，使代码可读。

**执行步骤**：

1. **阅读所有源文件**：.c / .h / .s（汇编）/ Makefile / 链接器脚本 / 配置文件
2. **检测并修复编码问题**：
   - 检查注释是否乱码 → 检测原始编码（中文项目尝试 gb2312/gbk）
   - 使用 Python 批量转换为 UTF-8：`data.decode('gbk').encode('utf-8')`
3. **生成系统分析报告**，内容包含：
   - 项目概述（MCU 型号、编译器、版本、版权）
   - 文件结构图及每个文件的用途
   - 硬件资源分配（ADC 通道、定时器、串口、GPIO）
   - 软件模块分解（每个源文件一个子章节）
   - 系统工作流程（启动序列、主循环、中断结构）
   - 优缺点分析（安全性、可维护性、可移植性、性能）
   - 优化建议（按高/中/低优先级排列）
   - 版本历史与关键技术参数

**输出**：`系统分析报告.md`

---

### 第二阶段：代码修订（安全性 + 可维护性 + 可移植性）

**目标**：修复安全问题、降低耦合、消除重复、提升可移植性。

**安全性修复**（优先级最高）：

| 修复项 | 方法 |
|---|---|
| 启用看门狗 | `_FWDT(WDT_OFF)` → `_FWDT(WDT_EN)` |
| EEPROM/Flash 写入保护 | 添加 DISI 指令 + 保存/恢复中断状态 + 提升 IPL |
| 提取公共保护逻辑 | 封装为 `EnterWriteProtect()` / `ExitWriteProtect()` 辅助函数 |

**可维护性修复**：

| 修复项 | 方法 |
|---|---|
| 状态变量枚举化 | `unsigned int step11` → `eStepState step11` |
| 消除重复代码 | 提取重复块为辅助函数（目标消除 >100 行重复） |
| 全局变量 static 化 | 扫描 extern 引用，未跨文件引用的变量添加 `static` |
| 魔数常量化 | 将 PULSEWIDTH、LT、START_DELAY_CNT 等换为 #define 命名常量 |

**可移植性修复**：

| 修复项 | 方法 |
|---|---|
| Makefile 跨平台 | 工具路径用变量、自动检测 OS、分离 build/output 目录 |
| 移除硬编码 | 删除 Windows 路径、`.exe` 后缀、反斜杠路径分隔符 |

**输出文件夹**：`_refactored/`
**输出文档**：`修订说明.md`（含每项修改的前后代码对比和原因说明）

---

### 第三阶段：全面注释

**目标**：每个函数和关键代码块都有清晰的解释性注释。

**注释规范**：

| 注释类型 | 内容要求 |
|---|---|
| 函数头注释 | 功能、参数、返回值、算法说明、调用上下文 |
| 关键语句注释 | 解释"为什么"而非"是什么" — 非显而易见的逻辑、硬件约束、算法原理 |
| 数据结构注释 | 每个结构体字段的含义和单位 |
| 模块头注释 | 文件用途、MCU 目标、编译器、修订历史 |
| 注释语言 | 匹配项目现有注释语言（中文项目 → 中文注释） |

**输出**：所有源文件在同文件夹中覆盖为带注释版本。

---

### 第四阶段：流程优化

**目标**：在不改变架构的前提下提升运行时行为。

| 优化项 | 说明 |
|---|---|
| **任务调度分层** | 将单一 10ms 循环拆分为 1ms/10ms/100ms 三层：1ms=快速保护(ISR 中)、10ms=核心控制(ADC/保护/状态机/软起动)、100ms=慢速任务(诊断/统计/IO扫描) |
| **保护快速通道** | 在 ISR 中增加极端条件的即时故障检测（如 6×额定过流 → 1ms 内响应） |
| **逻辑简化** | 用统一条件函数替换多路径（>100行）if-else 链 |
| **通讯事件驱动** | 仅在数据标志置位时调用 UART 处理，不每 10ms 轮询 |
| **运行日志** | 利用空闲 EEPROM/Flash 空间实现滚动日志（最近 N 次起动 + M 次故障） |

**输出文件夹**：`_V20.03_optimized/`（或下一版本号）
**输出文档**：`优化说明.md`（含优化原因、前后对比表、兼容性说明）

---

### 第五阶段：架构优化

**目标**：重构代码结构以提升可维护性，并添加高级功能。

| 优化项 | 说明 |
|---|---|
| **状态机表驱动重构** | 用状态处理函数数组 + 转移表替代大型 if-else 链 |
| **查表法简化** | 用数据驱动的查找表替代重复的 switch/if-else（如触发脉冲序列） |
| **闭环控制升级** | 在固定步长算法旁增加 PI/PID 控制器选项，通过 EEPROM 标志切换 |
| **参数完整性** | 对配置参数增加 CRC16 校验 + 自动备份/恢复 |
| **故障自诊断** | 增加传感器(CT/PT断线)、执行器(晶闸管老化/接触器粘连)、参考源(ADC电压)的在线健康检查 |

**输出文件夹**：`_V20.04_optimized/`
**输出文档**：`修订说明及结论.md`（含跨版本累计改进汇总）

---

### 第六阶段：文档输出

**目标**：产出全面的流程对比和功能文档。

当用户需要详细流程分析时，产出以下三份文档：

1. **起动/停机流程对比** — 原始 vs 修订的判定逻辑、状态转移图
2. **控制算法流程** — 恒流/PID/软起动控制回路的详细流程
3. **完整系统流程** — 所有子系统（初始化、调度、中断、状态机、保护、通讯、数据采集、EEPROM 布局）

每份文档应包含：
- ASCII 流程图（前后对比）
- 代码片段对比
- 时序图（相关场景）
- 参数参考表

---

## 输出规范

### 文件夹命名

```
项目名_V<major>.<minor>_<阶段>/
├── src/           # 源文件
├── h/             # 头文件
├── gld/           # 链接器脚本（如适用）
├── Makefile       # 构建系统
├── *.md           # 文档
```

### 文档命名

| 文档 | 对应阶段 |
|---|---|
| `系统分析报告.md` | 第一阶段：系统分析 |
| `修订说明.md` | 第二阶段：代码修订 |
| `优化说明_V<版本>.md` | 第四阶段：流程优化 |
| `V<版本>_修订说明及结论.md` | 第五阶段：架构优化 |
| `<主题>流程详解.md` | 第六阶段：流程文档 |
| `调度架构修订对比详解.md` | 调度架构对比 |
| `后续优化建议.md` | 未来优化路线图 |

### 修订文档模板结构

```markdown
# 标题

## 一、概述
## 二、各优化详细说明
  - 问题原因
  - 修改位置
  - 修改前（代码）
  - 修改后（代码）
  - 效果
## 三、修改文件清单
## 四、功能对比总表
## 五、兼容性说明
## 六、验证建议
```

---

## 嵌入式 C 特别注意事项

处理 MCU 固件时必须遵守：

- **绝不删除硬件初始化序列**，除非明显冗余
- **保持中断优先级分配不变** — 它们是为特定应用调优的
- **EEPROM/Flash 写入必须原子化** — 修改写入函数前必须添加保护
- **位域结构体**（如 `unsigned field:1`）依赖编译器 — 注释中标注但不改变布局
- **编译器内建函数**（如 `__builtin_mulss`、`__attribute__((interrupt))`）是平台特定的 — 文档化，不替换
- **ASM 块**（如 `asm("CLRWDT")`）必须原样保留
- **熔丝配置位**（如 `_FWDT()`、`_FOSC()`）必须与硬件设计匹配

---

## 常用修复模式速查

```c
// 1. 看门狗启用
// 修改前: _FWDT(WDT_OFF);
// 修改后: _FWDT(WDT_EN);

// 2. EEPROM 写入保护
// 修改前: SET_CPU_IPL(7); ... 写入 ... SET_CPU_IPL(3);
// 修改后: 保存IPL/IEC0 + __builtin_disi(0x3FFF) + SET_CPU_IPL(7) + 写入 + 恢复

// 3. 状态机枚举化
// 修改前: unsigned int step11 = 0;
// 修改后: eStepState step11 = STEP_WAIT;

// 4. 重复代码 → 辅助函数
// 修改前: 100+ 行代码在 2+ 个函数中重复
// 修改后: static unsigned int PackInputBits(void) { ... }

// 5. 全局 → static
// 修改前: unsigned int delayCnt;
// 修改后: static unsigned int delayCnt;
```

---

## MPS 项目实践成果（参考案例）

详细案例见 skill 内置参考文件 `references/mps_example.md`。

### 累计改进汇总（V20.02 → V20.04）

| 类别 | 项数 | 具体改进 |
|---|---|---|
| **安全性** | 3 | 看门狗启用、EEPROM 写入保护、过流快速通道 |
| **可维护性** | 6 | UTF-8 编码、枚举化、去重、static 化、状态机表驱动、查表简化 |
| **可移植性** | 1 | Makefile 跨平台 |
| **可追溯性** | 1 | EEPROM 运行日志 |
| **性能** | 3 | 调度分层、事件驱动通讯、PI 闭环控制 |
| **可靠性** | 2 | 参数 CRC 校验+备份、故障自诊断 |
| **合计** | **16** | |

### 代码规模演变

| 版本 | 文件数 | 说明 |
|---|---|---|
| V20.02 原始 | 13 文件 | GBK 编码，无注释，6 项代码问题 |
| V20.02 refactored | 13 文件 | 6 项代码修订 |
| V20.03 optimized | 14 文件 | +5 项流程优化 + EElog.c |
| V20.04 optimized | 16 文件 | +5 项架构优化 + Diagnose.c |

---

## Skill 文件结构

```
software-revision/
├── SKILL.md                        # Skill 主文件（英文，含触发条件和工作流定义）
└── references/
    └── mps_example.md              # MPS 项目完整实践案例
```

### 安装位置

- **全局安装**：`~/.claude/skills/software-revision/`
- **可分发包**：`software-revision.skill`（zip 格式，可分享安装）

---

*文档生成日期：2026-05-24*
*基于 MPS_DSP 项目 V20.02 ~ V20.04 四个版本的完整实践总结*
