# DSPIC30F5011 高压晶闸管软起动程序框架与流程问题分析报告

## 1. 分析范围与方法
- 代码范围：`src/` 下主流程、初始化、保护逻辑、采样中断、触发脉冲与状态机相关模块。
- 重点关注：
  1) 程序框架（模块职责与调用关系）；
  2) 执行逻辑（主循环/中断/状态机）；
  3) 流程风险与潜在 BUG（边界条件、时序、数值安全、可维护性）。

---

## 2. 程序总体框架

### 2.1 模块分层
1. **初始化层**
   - `main()` 里完成系统上电后初始化：参数加载、IO、定时器、ADC、IC、串口、CN 等。
   - 参数主入口在 `StateInit()`，从 EEPROM 读取保存参数并映射到 `StartParams/ProtectParams/Functionswitch`。

2. **时间调度层（软实时）**
   - `T1` 中断每 1ms 置位 `Ms1/Ms5/Ms10/AdMs100` 等软件时基标志；
   - 主循环根据这些标志分发任务（100ms、10ms 等）。

3. **测量与同步层**
   - `_ADCInterrupt`：采样电压电流、滑窗累计 RMS/PF 所需量；
   - `_IC4Interrupt`：过零捕获、周期估计、信号质量评估；
   - `_T5Interrupt/_T2Interrupt`：按触发角与脉宽输出晶闸管门极脉冲序列。

4. **控制与保护层**
   - `RelayFlow()`：主状态机（WAIT/READY/START_DELAY/START/RUN/...），驱动起停流程；
   - `SoftStart()/CurrentCheck()/SoftStop*()`：起动/停机控制；
   - `Protect()`：按状态选择欠压、过压、过流、不平衡、零序、温度等保护。

5. **通信层**
   - Modbus/串口收发与寄存器更新在主循环 10ms 分支中执行。

---

## 3. 执行逻辑（时序视角）

1. **上电阶段**
   - `main()` 先 `Delay60ms()`，然后执行 `while(!RCONbits.BOR);`，再初始化外设并进入主循环。

2. **主循环调度**
   - 100ms：无电检测、启动间隔保护、双曲线热过载、不平衡判据、输入采样；
   - 10ms：ADC降采样计算、保护判定、继电器流程、软起动/软停、相位比较、频率检测、通信处理。

3. **中断并行执行**
   - ADC中断持续刷新滤波/平方和；
   - 过零中断 + T5/T2 定时器链路负责门极脉冲时序；
   - T1 中断提供系统节拍并处理 Modbus 帧间隔计时。

---

## 4. 发现的问题与潜在 BUG

> 下面按“问题 -> 影响 -> 建议”给出。

### 问题 1：上电等待 BOR 标志可能导致系统卡死
- **位置**：`main()` 中 `while(!RCONbits.BOR);`
- **影响**：如果 BOR 标志位未按预期置位，程序会永远停在此处，系统无法进入初始化和运行。
- **建议**：增加超时退出或改为“读取并记录 BOR 原因”而不是阻塞等待。

### 问题 2：对无符号参数做 `<0` 判断是无效逻辑
- **位置**：`StateInit()` 对 `MainParams.SaveParams[x]`（`unsigned int`）多处做 `<0` 校验。
- **影响**：这些分支永远不会成立，导致参数下限保护缺失，容易掩盖异常参数。
- **建议**：改为明确的上下限区间校验，或先转为有符号再判断。

### 问题 3：停机斜坡差值计算分支写法错误，且存在下溢风险
- **位置**：`StateInit()` 中 `StopDif` 的 if/else 两分支完全相同，均为 `StopEnd - StopHead`。
- **影响**：当 `StopEnd < StopHead` 时，`unsigned long` 下会发生下溢并变成超大值，导致停机斜坡步进异常。
- **建议**：按业务意图区分增/减坡；必要时使用有符号差值并做饱和限制。

### 问题 4：功率因数计算存在除零风险
- **位置**：`AdcDescend()` 中 `cos_value = siAdTemp10 / (FILTER_N1 * Ps_value)`。
- **影响**：当电压或电流极低时 `Ps_value` 可能为 0，触发除零异常或产生无穷/NaN，后续逻辑被污染。
- **建议**：对分母设置最小阈值（如 `<eps` 则跳过更新或置默认值）。

### 问题 5：电流不平衡计算存在除零风险
- **位置**：`UnbalanceIProtect()` 中 `aveI = sum/3` 后执行 `IaIb / aveI`。
- **影响**：轻载、停机边界或采样异常时 `aveI` 可能为 0，导致除零。
- **建议**：先判断 `aveI==0`，直接返回或置为安全默认值。

### 问题 6：Modbus 3.5T 帧间隔阈值与注释/协议意图不一致
- **位置**：`_T1Interrupt()` 中 `MB.Timer35T > 10`（1ms节拍，即约10ms）触发帧结束。
- **影响**：9600bps 下 3.5字符时间约 4ms 左右，10ms 判帧过慢，可能造成吞吐下降或粘帧容忍过大。
- **建议**：按波特率动态计算 3.5T，或至少按固定波特率将阈值校准到接近 3.5T。

### 问题 7：共享变量的并发一致性风险（主循环与 ISR）
- **位置**：`SysTime`、`AdcParams`、`StartState`、`MB` 等在中断与主循环间共享。
- **影响**：部分字段未声明 `volatile`，在高优化等级下可能出现读取缓存、时序竞态、偶发逻辑抖动。
- **建议**：
  - ISR 与主循环共享的标志/计数器使用 `volatile`；
  - 多字节复合数据读取时加临界区或双缓冲快照。

### 问题 8：信号质量判断使用 `abs()` 但未显式包含标准声明头
- **位置**：`PhyVol.c` 中 `check_signal_quality()` 调用 `abs()`。
- **影响**：老编译器/严格告警配置下可能出现隐式声明告警，影响可移植性与可维护性。
- **建议**：显式包含 `<stdlib.h>`，并对整型宽度做一致性约束。

---

## 5. 流程层面的改进建议（优先级）

### P0（建议立即处理）
1. **除零与下溢防护**：问题 3/4/5。
2. **上电阻塞点容错**：问题 1。
3. **无效参数判断修正**：问题 2。

### P1（建议版本内处理）
1. **Modbus 3.5T 计时按波特率自适应**：问题 6。
2. **中断共享数据并发规范化**：问题 7。

### P2（工程质量提升）
1. 统一关键路径的“输入校验 + 饱和处理 + 故障回退”模板；
2. 给状态机 `step11/step4/step5` 增加非法状态保护与统计；
3. 在关键保护动作处增加事件日志（时间戳、状态、采样快照）。

---

## 6. 建议的最小验证清单
1. 上电 BOR 异常场景：确认不会卡死在主循环前。
2. 空载/低压/低流场景：验证 PF 与不平衡计算不会除零。
3. 停机参数边界：`StopHead/StopEnd` 逆序输入时，停机斜坡稳定。
4. 9600/19200bps Modbus 压测：验证帧边界识别与丢包率。
5. 中断高负载场景：确认主循环标志读取与状态迁移无抖动。

---

## 7. 结论
该工程整体架构是典型的 **“中断采样 + 1ms 软时基调度 + 状态机驱动控制保护”**，框架思路正确、模块职责相对清晰；但在**边界条件防护（除零/下溢）、参数校验有效性、上电容错、通信时序精度**方面存在可导致现场偶发故障或误动作的风险点。建议优先修复 P0 项，再进行 P1 的通信与并发一致性整治。

---

## 8. 直接打补丁修复清单（先做 P0）

> 目标：你可以按下面条目直接在对应函数内修改；每条都给了“建议代码改法 + 回归测试点”。

### P0-1：去掉上电死等 BOR 的阻塞
- **文件/函数**：`src/MainCont.c` -> `main()`
- **现状**：`while(!RCONbits.BOR);`
- **建议改法（示意）**：
  1. 将无限等待改为有限超时（例如 200ms）；
  2. 超时后记录一次告警标志（可映射到故障字或调试寄存器），继续初始化。

```c
unsigned int bor_wait = 0;
while(!RCONbits.BOR && bor_wait < 200) {
    Delay1ms();
    bor_wait++;
}
if(!RCONbits.BOR) {
    // 记录 BOR 未置位事件（例如 Fault/Buf/DebugFlag）
}
```

- **回归测试点**：
  1. 正常上电：系统按原流程进入 READY；
  2. 人为模拟 BOR 不置位：系统不死锁，仍进入初始化；
  3. 记录标志可通过串口/寄存器读取。

---

### P0-2：修复 `SaveParams` 的无效 `<0` 判断
- **文件/函数**：`src/init5011.c` -> `StateInit()`
- **现状**：对 `unsigned int SaveParams[]` 使用 `<0` 分支。
- **建议改法**：统一改成区间钳位函数。

```c
static unsigned int clamp_u16(unsigned int v, unsigned int lo, unsigned int hi, unsigned int dft)
{
    if(v < lo || v > hi) return dft;
    return v;
}

MainParams.SaveParams[3] = clamp_u16(MainParams.SaveParams[3], 1, 2000, 200);
MainParams.SaveParams[4] = clamp_u16(MainParams.SaveParams[4], 0, 3, 0);
```

- **回归测试点**：
  1. EEPROM 参数写入边界外值（如 0xFFFF）：上电后被钳位到默认值；
  2. 合法值不被篡改；
  3. Modbus 读回参数与期望一致。

---

### P0-3：修复停机斜坡 `StopDif` 下溢
- **文件/函数**：`src/init5011.c` -> `StateInit()`
- **现状**：if/else 两分支相同，`StopEnd < StopHead` 时无符号下溢。
- **建议改法**：使用有符号差值并限制步进方向。

```c
long stop_diff = (long)StartParams.StopEnd - (long)StartParams.StopHead;
StartParams.StopDif = (unsigned long)((stop_diff >= 0) ? stop_diff : (-stop_diff));
StartParams.StopDif <<= 10;
if(StartParams.StopTime == 0) StartParams.StopTime = 1;
StartParams.StopStep = StartParams.StopDif / StartParams.StopTime;
```

> 如果业务要求“只允许某一方向斜坡”，可在参数加载时直接交换 `StopHead/StopEnd` 或拒绝非法组合。

- **回归测试点**：
  1. `StopHead > StopEnd` 与 `StopHead < StopEnd` 两组参数均能平滑停机；
  2. 不出现“停机角突跳到极值”；
  3. 示波器观察触发角变化连续。

---

### P0-4：修复 PF 计算分母为 0
- **文件/函数**：`src/Current.c` -> `AdcDescend()`
- **现状**：`cos_value = siAdTemp10 / (FILTER_N1 * Ps_value)`，分母可能为 0。
- **建议改法**：增加分母阈值保护和 `cos` 限幅。

```c
long denom = (long)AdcParams.FILTER_N1 * (long)AdcParams.Ps_value;
if(denom <= 0) {
    AdcParams.cos_value = 0.0;
} else {
    AdcParams.cos_value = (double)AdcParams.siAdTemp10 / (double)denom;
    if(AdcParams.cos_value > 1.0) AdcParams.cos_value = 1.0;
    if(AdcParams.cos_value < -1.0) AdcParams.cos_value = -1.0;
}
```

- **回归测试点**：
  1. 空载/低压（接近 0）时不触发异常；
  2. PF 值稳定、无 NaN/Inf；
  3. 与正常负载下历史算法结果偏差在可接受范围。

---

### P0-5：修复不平衡计算除零
- **文件/函数**：`src/Protect1.c` -> `UnbalanceIProtect()`
- **现状**：`realUnblance = IaIb / aveI`，`aveI` 可能为 0。
- **建议改法**：在 `aveI` 小于阈值时跳过保护累积。

```c
aveI = __builtin_divud(sum, 3);
if(aveI < 1) {
    unbalanceICnt1 = 0;
    unbalanceICnt2 = 0;
    return;
}
```

- **回归测试点**：
  1. 空载、低流、起停切换边界不报错；
  2. 人为注入三相不平衡时仍可正确触发保护；
  3. 保护延时计数逻辑不被破坏。

---

## 9. P0 修改后的统一回归方案（建议一次性执行）
1. **基础启动回归**：连续上电/下电 50 次，确认无卡死。
2. **参数鲁棒性回归**：EEPROM 参数注入越界值，确认上电钳位正确。
3. **软停动态回归**：不同 `StopHead/StopEnd/StopTime` 组合做 20 组测试。
4. **低压空载回归**：确认 PF 与不平衡计算无除零异常。
5. **保护功能回归**：过流、欠压、不平衡、零序保护逐项触发与复位。
6. **通信兼容回归**：P0 不改通信逻辑，但需确认 Modbus 回读无回归。